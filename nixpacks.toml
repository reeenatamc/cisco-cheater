[phases.setup]
aptPkgs = ['tesseract-ocr', 'tesseract-ocr-spa', 'tesseract-ocr-eng', 'chromium', 'chromium-driver']

[phases.build]
cmds = ['python manage.py collectstatic --noinput']

[start]
cmd = 'gunicorn cheater.wsgi:application'
